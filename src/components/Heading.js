import React from "react"
import '../styles/heading.css'
const Heading = ({text}) => {
    return (
        <div className="heading" >
            {text}
        </div>
    )
}

export default Heading